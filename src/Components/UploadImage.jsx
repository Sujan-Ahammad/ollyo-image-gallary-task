const UploadImage = () => {

    const uploadImages=()=>{

        
    }


  return (
    <div>
      <button onClick={uploadImages}>Add Images</button>
    </div>
  );
};

export default UploadImage;
